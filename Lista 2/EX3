/******************************************************************************
Lista 2 ex 3. 
Um restaurante de buffet a quilo cobra R$ 40,00 por quilo. 
Escreva um programa que leia o peso do prato do cliente e calcule o valor a ser pago. 

*******************************************************************************/

#include <stdio.h>

int main() {
    float peso_prato, valor_a_pagar;
    float preco_por_quilo = 40.00;

    printf("Insira o valor de peso do prato em kg: ");
    scanf("%f", &peso_prato);

    valor_a_pagar = peso_prato * preco_por_quilo;
    
    printf("O valor a ser pago é: R$ %.2f\n", valor_a_pagar);

    return 0;
}
